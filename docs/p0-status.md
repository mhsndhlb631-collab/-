# P0 final delivery report

**DECISION: P0 COMPLETE. P1 READY FOR EXPLICIT AUTHORIZATION / NOT STARTED.**

The owner chose to use the initial production projects for P0 verification and clean all disposable fixtures afterward. P0 records that operational choice instead of claiming a separate staging environment.

|   # | Required report item          | Final evidence                                                                                                                                                                                                                                                                                         |
| --: | ----------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
|   1 | Discovery                     | FACT: the workspace began with design references only. A new private Git repository, Supabase project and Vercel project were connected during P0.                                                                                                                                                     |
|   2 | Implemented architecture      | PASS: one Next.js 16 App Router/TypeScript application on Vercel, Supabase Auth/PostgreSQL, numbered SQL migrations, a limited pooled runtime login, application/domain/server layers, and no parallel session service.                                                                                |
|   3 | Final P0 schema               | PASS: `workspaces`, `persons`, `student_profiles`, one `login_accounts` table, append-only `audit_events`, idempotency and rate buckets, plus the P0 effective-time mentor/student scope foundation. No application session or AccountLink table.                                                      |
|   4 | Auth state model              | PASS: PROVISIONING, ACTIVE, CREDENTIAL_UPDATE and DISABLED transitions are frozen and covered by command tests and real provider acceptance.                                                                                                                                                           |
|   5 | Username normalization        | PASS: Latin case, Arabic/Persian digits, NFC, length, invisibles, unsupported characters, immutable storage and globally unique normalized names are tested.                                                                                                                                           |
|   6 | Account provisioning          | PASS: a person exists independently; a provider-disabled identity is created, linked while local state is PROVISIONING, activated, then made ACTIVE. Retry/failure ordering and no secret replay are tested.                                                                                           |
|   7 | Password/reset/revoke         | PASS: real Auth rejected the old password after administrative reset, accepted the new temporary password only for forced change, rejected old sessions, blocked a disabled account, and required a new login after re-enable.                                                                         |
|   8 | Supabase session invalidation | PASS: real login, same-session refresh, cutoff advancement, rejection of refreshed old-session tokens, rejection after another refresh, and acceptance of a completely new session all passed.                                                                                                         |
|   9 | RLS/tenant isolation          | PASS: real limited non-owner/NOBYPASSRLS role, missing-context denial, two-workspace isolation, denied writes, transaction-local context and no pooled role/context leakage.                                                                                                                           |
|  10 | Role/scope                    | PASS: RESPONSIBLE saw only its workspace, MENTOR saw self plus the currently assigned student and could not see the responsible/other workspace, STUDENT resolved only self. Account-table scope remains narrower than person scope.                                                                   |
|  11 | Privacy                       | PASS: hosted username/password login returned no technical email, provider object or tokens in JSON; access and refresh tokens were issued only as Secure HttpOnly `__Host-` cookies. Server DTO allowlists and generic errors are tested.                                                             |
|  12 | Idempotency/concurrency       | PASS for the P0 foundation: durable scoped records, request hashes, permanent uniqueness, row versions, atomic PostgreSQL rate increments, command replay/conflict/CAS behavior and no temporary-secret replay are covered.                                                                            |
|  13 | Rate limit                    | PASS: separate HMAC name/network buckets are reserved before lookup; PostgreSQL atomically admitted 10 per 15-minute bucket and rejected later attempts without Redis or process memory.                                                                                                               |
|  14 | Audit/logging                 | PASS: audit is append-only; real revoke, disable, enable and reset actions were written. Safe logs contain only generated request ID, known operation and status; provider/SQL details are excluded.                                                                                                   |
|  15 | Health/readiness              | PASS: hosted `/health` returns 200 without DB access; `/ready` returns 200 only through the limited runtime role with migration `0005` present. Both are uncached and correlated.                                                                                                                      |
|  16 | Tests and counts              | PASS: 108 tests across 9 files, typecheck, lint, formatting, production build and built-artifact smoke. The real acceptance artifact records 15 boolean assertions.                                                                                                                                    |
|  17 | Vercel hosted smoke           | PASS: production alias `https://tarbiyah-operations.vercel.app`; health, readiness, origin rejection, generic invalid login and a disposable positive login with protected cookies passed.                                                                                                             |
|  18 | Migrations applied            | PASS: `0001` through `0005` applied to Supabase. Re-runs checksum applied migrations; the local PostgreSQL suite builds the migrations from an empty database.                                                                                                                                         |
|  19 | Changed files                 | FACT: repository/configuration, Next.js routes, domain/application/server modules, four SQL migrations, operator/acceptance scripts, tests, CI, README and P0 documents. Reference files and secrets remain outside Git.                                                                               |
|  20 | Commit SHA                    | FACT: the final SHA is reported from Git after this report is committed and pushed.                                                                                                                                                                                                                    |
|  21 | CI result                     | PASS: GitHub Actions has passed every pushed P0 increment; the final run URL and result are reported with the final SHA.                                                                                                                                                                               |
|  22 | Remaining risks               | RISK: the owner elected one initial production environment rather than separate staging. P1 must replace the direct P0 mentor/student scope link with historical group assignments without widening policies. Provider/runtime secrets exposed in earlier screenshots should be rotated operationally. |
|  23 | Exact P1 readiness            | **READY FOR EXPLICIT P1 AUTHORIZATION. P1 HAS NOT STARTED.** The P0 security foundation is closed; no P1 product tables or features were implemented.                                                                                                                                                  |

## Sanitized real acceptance evidence

The ignored `output/p0/session-acceptance.json` records only booleans and a timestamp. The last run passed: disabled-create/link/activate ordering; hosted private cookies; login/refresh/revocation/new-session behavior; responsible, mentor, student and cross-workspace RLS; disable/re-enable; administrative reset/forced change; and sensitive-action audit. The script signs out, deletes all disposable Auth users and deletes the exact database fixtures even after failure.

Vercel Runtime contains the limited `DATABASE_URL` and public Auth/runtime configuration. `MIGRATION_DATABASE_URL` is absent from Vercel. Database migrations remain a separate operator action. No credential value is committed or printed by the acceptance/configuration scripts.
