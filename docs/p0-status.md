# P0 delivery status — local foundation checkpoint

**DECISION: P0 INCOMPLETE. P1 NOT READY / NOT STARTED.**

This is a checkpoint report, not the requested final P0 completion report. Supabase and Vercel projects do not exist yet, as confirmed by the user. Auth-dependent work is held at the real session spike gate. No credentials, cloud resources, account provisioning or real-student data were created.

| #   | Required report item          | Current evidence / status                                                                                                                                                                                                                                                                               |
| --- | ----------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | Discovery                     | FACT: empty application workspace; repository initialized; Node 24 and Git available; no cloud configuration or remote. User confirmed providers will be set up later.                                                                                                                                  |
| 2   | Implemented architecture      | FACT: one Next.js 16.3.4 App Router application; React 19.2.8, TypeScript, postgres-js with prepared statements disabled, Drizzle adapter available. Minimal Arabic RTL shell and two health routes. Auth services and data routes not implemented.                                                     |
| 3   | Final P0 schema               | DECISION: frozen target in p0-schema.md; 0001 SQL implements metadata, workspace/person/student/account/audit/idempotency/rate-bucket foundation. Real Auth accessor and allow policies pending spike.                                                                                                  |
| 4   | Auth state model              | DECISION: frozen in p0-auth-and-scope.md. Local command orchestration implements guarded provisioning/reset/disable/enable/revoke boundaries behind repository/provider interfaces. Real adapters remain gated.                                                                                         |
| 5   | Username normalization        | FACT: unit cases cover Latin case, Arabic/Persian digits, NFC, length, spelling distinctions, invisible/unsupported characters; SQL rejects noncanonical values and global duplicates. No live username login yet.                                                                                      |
| 6   | Provisioning                  | PASS for the core provider ordering: real Supabase created disabled Auth identities, linked each while the application account remained PROVISIONING, activated Auth, then marked the linked account ACTIVE. Temporary fixtures were removed. Failure-injection/retry behavior remains covered locally. |
| 7   | Password/reset/revoke         | FACT: policy tests reject blocked/expired/revoked sessions. Local reset orchestration requires recent RESPONSIBLE authentication, blocks in CREDENTIAL_UPDATE on provider failure and never replays a completed secret. BLOCKED: real adapters and flows.                                               |
| 8   | Supabase session invalidation | PASS: real Supabase proved normal login, refresh preserving the original session ID, audited advancement of revoked_before, rejection of the refreshed token and a later refresh from that old session, and acceptance of a new login with a new session ID. The accessor and fixtures were removed.    |
| 9   | RLS/tenant isolation          | FACT: real Supabase Transaction pooler acceptance proves transaction-local selection of the limited role, default-deny RLS reads, denied domain writes, and no role leakage afterward. Positive role/scope policies remain pending.                                                                     |
| 10  | Role/scope                    | FACT: pure policy covers RESPONSIBLE workspace authority, MENTOR assigned-only and STUDENT self-only. NOT IMPLEMENTED: authenticated endpoints, effective-time scope fixtures and full positive RLS policies.                                                                                           |
| 11  | Privacy                       | FACT: DTO allowlist strips extra fields in unit test; no private endpoint exists. BLOCKED: real SSR/token synthetic-email privacy and full HTTP/Server Action acceptance.                                                                                                                               |
| 12  | Idempotency/concurrency       | FACT: table, hash, permanent uniqueness keys and row_version foundation exist. NOT IMPLEMENTED: operational replay, CAS command services, distributed provisioning/reset races.                                                                                                                         |
| 13  | Rate limit                    | FACT: SQL atomic upsert enforces 10 attempts per 15-minute HMAC bucket. Login orchestration reserves separate HMAC buckets for normalized name and trusted network key before lookup/Auth. BLOCKED: adapter and real multi-instance concurrency.                                                        |
| 14  | Audit/logging                 | FACT: append-only audit schema and tests; safe HTTP logs permit only generated request ID, known operation and status. NOT IMPLEMENTED: normal account command audit, because commands are gated.                                                                                                       |
| 15  | Health/readiness              | FACT: /health no DB dependency; /ready fails closed on missing/unsafe role or absent foundation migration. Readiness is infrastructure status only.                                                                                                                                                     |
| 16  | Tests and counts              | Verification results are recorded below. Local tests do not satisfy hosted acceptance.                                                                                                                                                                                                                  |
| 17  | Vercel hosted smoke           | BLOCKED: no project/deployment.                                                                                                                                                                                                                                                                         |
| 18  | Migrations applied            | PASS: 0001 applied to the real Supabase project. A second run confirmed the stored checksum. The same migration applies to a fresh local PostgreSQL engine in integration tests.                                                                                                                        |
| 19  | Changed files                 | README; package/lock/config files; src/app, src/domain, src/server; db/migrations and experimental db/spikes; scripts; tests; docs; .github/workflows/ci.yml. Original reference PDF left outside Git.                                                                                                  |
| 20  | Commit SHA                    | See Git history / delivery message; this report is committed with the local checkpoint, not a P0-complete commit.                                                                                                                                                                                       |
| 21  | CI result                     | Workflow prepared. BLOCKED: no remote repository, no hosted CI run. Local command results are reported separately.                                                                                                                                                                                      |
| 22  | Remaining risks               | Original-session support; active-unlinked provisioning interruption; synthetic-email in provider/SSR data; atomic state/audit/retry commands; full RLS scopes and pooled context; hosted runtime and recovery acceptance all remain unproven.                                                           |
| 23  | Exact P1 readiness            | **NOT READY. Do not start P1.** Create dedicated staging projects, pass the real spike, implement remaining P0 services/security tests, then complete CI/deployment/hosted gates.                                                                                                                       |

## Verification results

FACT: The last complete local gate passed **101 tests across 6 files**, typecheck, lint, format, production build and built-artifact smoke. Runtime URL derivation adds four unit cases; the full suite is rerun before this increment is committed.

FACT: Real Supabase evidence now includes clean migration application, idempotent checksum verification, creation of a non-owner/non-superuser/non-bypass/noinherit login, transaction-local selection of the limited capability role through port 6543, default-deny reads, denied writes, and no role leakage after the transaction. Acceptance scripts printed no credentials or connection strings.

FACT: Real Auth acceptance passed every LOCK 3 assertion. It also proved provider-disabled create -> local link -> provider activation ordering for disposable RESPONSIBLE and STUDENT identities. The script stored only boolean evidence, signed out, deleted both Auth users and exact database fixtures, and dropped its experimental auth.sessions accessor. Independent cleanup queries returned PASS for both fixture and accessor absence.

FACT: Running the real-session script without credentials exited 1 with a safe BLOCKED message. This validates the script's configuration guard only; it is not a real session test. Native Node 24 TypeScript execution is used for the operator scripts after the optional tsx loader hit a Windows sandbox user-info restriction; tsx was removed.

FACT: The local migration integration suite applied 0001 to a fresh PGlite database. Hosted migration, pooled PostgreSQL, distributed-login concurrency, auth integration, remote CI and Vercel smoke have **not run**. No skipped/mocked test is counted as passing a hosted gate.

## Next concrete step

DECISION: Set up the dedicated Supabase staging project and runtime/migrator roles using p0-setup.md, then run the real original-session spike. This is the next dependency gate, not a request to approve the product design again. No new auth/session platform will be introduced if the spike fails.
