# P7 delivery report — complete role journeys

**DECISION: P7 CLOSED. P8 NOT STARTED.**

| Gate                     | Final evidence                                                                                                                                                                                                                                             |
| ------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Role journeys            | RESPONSIBLE, MENTOR, and STUDENT each receive a role-shaped identity, today, program, and progress journey through hosted HTTP. Navigation exposes only the work available to that role.                                                                   |
| Self identity            | `GET /me` returns display name, login name, role, and workspace name without provider identity, token, session, or private mentoring data.                                                                                                                 |
| Password lifecycle       | Hosted acceptance proves temporary-password login returns `CHANGE_PASSWORD`, normal resources remain blocked, current-password verification succeeds, old sessions and password are rejected, a fresh login succeeds, and one safe audit event is written. |
| Logout                   | Logout terminates the current provider session when available, expires both HttpOnly cookies, remains safe for an already expired session, and the previous session is rejected.                                                                           |
| Scope and privacy        | Hosted acceptance covers two workspaces and proves student self scope, mentor scope, responsible-only report/audit denial, and server-shaped DTOs without internal fields.                                                                                 |
| RTL and mobile           | Arabic `lang`/`dir`, role navigation, 320px layout rules, internal horizontal scrolling where needed, 44px controls, reduced motion, and no page-level fixed-width dependency are present in the production artifact.                                      |
| Accessibility and states | Semantic navigation and main landmarks, skip link, visible keyboard focus, labelled forms, live status/alert text, and loading, empty, failure, retry, success, and session-expiry paths are implemented.                                                  |
| Database                 | Supabase migration `0030_p7_self_service_auth.sql` is applied with matching checksum. Password completion revokes sessions and records audit through a narrowly granted runtime function.                                                                  |
| Automated checks         | Formatting, lint, typecheck, production build, artifact smoke, and 132 tests across 12 files pass. GitHub Actions run `34466757229` passed.                                                                                                                |
| Hosted acceptance        | `output/p7/acceptance.json` passed all five sanitized evidence groups at `2026-09-10T10:32:04.804Z`; exact database and Auth fixtures were removed.                                                                                                        |
| Deployment               | Vercel production deployment for implementation commit `98176e1` is Ready and serves the main aliases.                                                                                                                                                     |

P7 is closed at implementation commit `98176e1`. P8 production readiness, load/security/recovery proof, observability, runbooks, environment separation, and the two-week pilot remain outside this delivery and have not started.
