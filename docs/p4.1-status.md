# P4.1 delivery report — automatic weekly finalization

**DECISION: P4.1 CLOSED. P5 NOT STARTED.**

P4.1 resolves the product-lock conflict left by historical P4. Weekly summaries no longer require `READY -> APPROVED`; the implemented lifecycle is `OPEN -> FINALIZED`, and corrections create immutable finalization revisions.

| Gate                  | Final evidence                                                                                                                                                                  |
| --------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Lifecycle             | Database and service accept only `OPEN` and `FINALIZED` operationally. Legacy weekly ready/approve/corrections routes were removed.                                             |
| Closing boundary      | Full evidence permits finalization before closing; after the workspace-local inclusive week end, missing evidence remains visible but does not prevent operational closing.     |
| Immutable history     | First finalization persists revision 1 and score 80. Corrected and republished exam evidence followed by amendment creates revision 2 and score 90 without changing revision 1. |
| Concurrency           | A stale amendment is rejected with HTTP 409 and `VERSION_CONFLICT`; the valid current version succeeds.                                                                         |
| Revision integrity    | The summary's `current_revision` is updated atomically with first finalization, preventing duplicate revision 1 during amendment.                                               |
| Privacy and isolation | Student-visible publication boundaries remain intact and cross-workspace week access returns 404.                                                                               |
| Audit                 | Finalization and amendment are written atomically with the required P4 learning actions; hosted acceptance passes the audit-count assertion.                                    |
| Database              | Supabase migrations `0024_add_finalized_value.sql` and `0025_finalize_transitions.sql` are applied. The transition and closing-boundary guards are tested.                      |
| Automated checks      | 123 tests across 11 files, formatting, lint, typecheck, production build, and artifact smoke pass.                                                                              |
| Hosted acceptance     | `output/p4/acceptance.json` passed all 15 sanitized evidence flags at `2026-09-09T19:05:52.214Z` and cleaned its exact fixtures.                                                |
| Deployment            | Vercel `/health` reported release `954a4bc2c453`; GitHub Actions run `34392840223` completed successfully.                                                                      |

The closing fix is commit `954a4bc2c453827bde82f04485ba4c7511b4169a`. P5 attention, actions, followups, cases, escalation, and outcome verification remain outside this delivery and have not started.
