# الملخص الشامل للمشروع — نظام التشغيل التربوي

> **نوع المستند:** مرجع تنفيذي وتشغيلي موحّد  
> **تاريخ الحالة:** 9 سبتمبر 2026  
> **حالة المنتج:** اكتملت المراحل P0–P5، ولم تبدأ P6–P8
> **آخر كود تحقّق منه هذا الملخص:** `954a4bc`
> **المستودع:** [mhsndhlb631-collab/-.git](https://github.com/mhsndhlb631-collab/-.git)  
> **النشر الحالي:** [tarbiyah-operations.vercel.app](https://tarbiyah-operations.vercel.app)

هذا المستند يصف ما هو موجود في الكود وقاعدة البيانات والنشر الآن، وما بقي من التصميم الكامل. عند التعارض، تكون الأولوية للقرار المعتمد الأحدث، ثم لعقد المرحلة، ثم للكود الفعلي. أي اختلاف بين هذه المستويات مسجل صراحة في قسم الانحرافات، ولا يُعامل كقرار جديد ضمنيًا.

## 1. هدف النظام

نظام التشغيل التربوي منصة عربية لإدارة دورة العمل كاملة داخل جهة تربوية واحدة أو أكثر. يبدأ من تعريف البرامج والخطط والدفعات والمجموعات، ثم تشغيل الجلسات والحضور، وتتبع الأوراد والعادات والقيم والأهداف، وإدارة المحتوى والتكليفات والاختبارات، وينتهي بالمتابعة والحالات وقياس أداء المربين والتقارير والتشغيل التجريبي.

المبدأ الأساسي هو أن كل قرار أو نتيجة قابلة للتتبع إلى دليلها ومنفذها ووقتها، مع فصل ما يراه الطالب عن الملاحظات التربوية الداخلية، وحفظ التاريخ عند النقل أو التصحيح بدل محوه.

## 2. الأدوار وحدودها

يدعم الإصدار V1 دورًا تشغيليًا واحدًا لكل حساب:

| الدور         | نطاق العمل الفعلي                                                                                       |
| ------------- | ------------------------------------------------------------------------------------------------------- |
| `RESPONSIBLE` | يدير الجهة وبرامجها وأشخاصها، ويستطيع تنفيذ أعمال المربي داخل الجهة عند الحاجة مع حفظ هويته كمنفذ فعلي. |
| `MENTOR`      | يعمل داخل المجموعات والطلاب المسندين إليه في الزمن المعني، ولا يكفي امتلاك معرف طالب للوصول إليه.       |
| `STUDENT`     | يرى نفسه فقط، وما نُشر له من خطة ومحتوى ونتائج وتغذية راجعة، ويسجل الإدخالات المسموح بها لنفسه.         |

لا توجد بنية أدوار متعددة للحساب في V1. عندما يتدخل المسؤول في عمل مربي، يسجل التدقيق أن المنفذ `RESPONSIBLE`، بينما تظل نسبة المسؤولية التاريخية للمربي الأساسي المسند وقت الواقعة دون تغيير.

### ما يراه الطالب

- الخطة والمحتوى المنشوران له.
- تكليفاته وإدخالاته وحالة مراجعتها.
- النتائج والتغذية الراجعة المنشورتان صراحة.
- تقدمه والتقييم الذاتي الخاص به.

### ما يبقى داخليًا

- الملاحظات التربوية الداخلية.
- المتابعات والحالات وأدلتها.
- تصنيف المخاطر والإسناد الداخلي.
- تقييم المربين وسجل التدقيق.
- النتائج والمسودات التي لم تُنشر.

الفصل يتم في قاعدة البيانات وخدمات الخادم وDTOs، وليس بإخفاء حقول في الواجهة فقط.

## 3. المعمارية المنفذة

| الطبقة          | التقنية والمسؤولية                                                                       |
| --------------- | ---------------------------------------------------------------------------------------- |
| الواجهة والخادم | Next.js 16 App Router، React 19، TypeScript 6، وواجهات RTL عربية.                        |
| العقود والتحقق  | Zod ومدخلات صارمة ترفض الحقول غير المعروفة.                                              |
| خدمات التطبيق   | خدمات مستقلة لكل مرحلة تطبق الانتقالات والصلاحيات والمعاملات والتدقيق.                   |
| البيانات        | PostgreSQL على Supabase، مع `postgres.js` وDrizzle ORM حيث يلزم.                         |
| المصادقة        | Supabase Auth خلف محول اسم دخول/كلمة مرور يعمل من الخادم.                                |
| الأمان          | مستخدم PostgreSQL محدود، سياق طلب محلي للمعاملة، وRLS مفعّل إجباريًا.                    |
| النشر           | Vercel على الرابط الحالي، مع Supabase للمصادقة وقاعدة البيانات.                          |
| التكامل المستمر | GitHub Actions على Node.js 24: تنسيق، lint، typecheck، اختبارات، build، وartifact smoke. |

المتصفح لا يختار `workspace_id` أو الدور أو `person_id` أو `student_id` الموثوق. الخادم يستنتجها من جلسة مصادق عليها، ثم يفتح معاملة بحساب التشغيل المحدود ويضبط سياق الحساب والجهة والجلسة محليًا للمعاملة. تنتهي المعاملة قبل إعادة الاتصال إلى المجمّع، لمنع تسرب نطاق مستخدم إلى طلب آخر.

لا يستخدم CRUD التربوي مفتاح `service_role`. اتصال migrations إداري منفصل ولا يوضع في Vercel Runtime.

## 4. القرارات المعتمدة التي تحكم التنفيذ

1. **نسخة مستقلة للدفعة:** عند إنشاء دفعة، تُنسخ خطة البرنامج وتعريفاتها. التعديلات اللاحقة على القالب لا تغير تاريخ الدفعة.
2. **تاريخ الإسناد لا يُمحى:** نقل الطالب أو المربي يغلق السجل القديم ويبدأ سجلًا جديدًا بوقت سريان.
3. **المسؤول يستطيع العمل كمربي:** داخل الجهة فقط، مع حفظ المنفذ الفعلي وعدم إعادة كتابة المربي التاريخي.
4. **الطالب لا يرى البيانات الداخلية:** النشر فعل صريح، والمسودة لا تظهر له.
5. **المفقود ليس صفرًا:** غياب البيانات يبقى ظاهرًا، ولا يدخل كصفر إلا إذا نصت قاعدة خطة معلنة على ذلك.
6. **التصحيح ينشئ تاريخًا:** السجلات المغلقة أو المنشورة أو المراجعة لا تُستبدل دون أثر؛ التصحيح يحفظ النسخة السابقة والسبب والمنفذ والوقت.
7. **كل كتابة حساسة ذرية:** التغيير والتدقيق ومنع التكرار يحدثون داخل معاملة واحدة حيث يمكن ذلك.
8. **الأمان دفاع متعدد الطبقات:** تحقق الخدمة، ونطاق قاعدة البيانات، وRLS الإجباري، ومنع صلاحيات Data API المباشرة.
9. **لا منصة جلسات موازية:** إبطال الجلسات يعتمد على جلسة Supabase الأصلية بعد إثباته عمليًا في P0.
10. **لا اعتماد أسبوعي إلزامي:** القفل النهائي اللاحق للتصميم يحدد دورة `OPEN → FINALIZED` وملخصًا مشتقًا تلقائيًا، ويراجع المسؤول الاستثناءات والتصحيحات والحالات المهمة فقط.

## 5. منهجية التعامل مع كل مرحلة

تُنفذ كل مرحلة كوحدة مغلقة يمكن إثباتها:

1. قراءة التصميم والأقفال الأحدث وتحديد ما يدخل المرحلة وما يبقى خارجها.
2. كتابة عقد مرحلة مجمّد يحدد النطاق والقرارات وبوابة الإغلاق.
3. إضافة migrations مرقمة لا تُعدّل بعد تطبيقها؛ يعاد التحقق من checksum عند التشغيل التالي.
4. بناء خدمة المجال أولًا، ثم HTTP/API، ثم واجهة التشغيل اللازمة.
5. تطبيق الصلاحيات مرتين: في الخدمة داخل المعاملة، وفي PostgreSQL عبر RLS.
6. استخدام `row_version` للتزامن المتفائل، وقفل الصف عند انتقالات الحالة الحساسة.
7. استخدام `Idempotency-Key` للأوامر التي قد تتكرر، مع hash للطلب ورفض إعادة المفتاح بمحتوى مختلف.
8. حفظ حدث تدقيق في نفس معاملة العملية الحساسة.
9. تشغيل الفحوص المحلية: التنسيق، lint، typecheck، الاختبارات، build، وartifact smoke.
10. تطبيق migrations على Supabase والتحقق من مستخدم التشغيل المحدود.
11. نشر Vercel، ثم اختبار المسار الكامل عبر HTTP على Vercel وSupabase الحقيقيين.
12. حذف fixtures التجريبية المحددة حتى عند فشل الاختبار، وحفظ دليل قبول منزوع الأسرار محليًا.
13. تحديث عقد الحالة، ثم commit وpush، ثم التحقق من GitHub Actions والصحة والاستعداد.

لا تُغلق مرحلة بسبب وجود واجهة تبدو صحيحة فقط. يجب أن تجتاز مساراتها الإيجابية، وعزل الجهة والنطاق، والخصوصية، والتزامن، والتدقيق، والتنظيف.

## 6. الحالة الإجمالية للمراحل

| المرحلة | النطاق                                          | الحالة              | الدليل المختصر                                                        |
| ------- | ----------------------------------------------- | ------------------- | --------------------------------------------------------------------- |
| P0      | الأساس، الهوية، المصادقة، RLS، التدقيق، التشغيل | **مكتملة**          | قبول حقيقي للجلسات والأدوار والعزل، migrations `0001–0005`.           |
| P1      | البرامج والخطط والدفعات والمجموعات والإسناد     | **مكتملة**          | نسخ خطة مستقل، نقل مؤرخ، وعزل الأدوار، migrations `0006–0008`.        |
| P2      | الجلسات والحضور وتسجيل التنفيذ                  | **مكتملة**          | roster مجمد، إغلاق كامل، تصحيح وتزامن، migrations `0009–0015`.        |
| P3      | التتبع والتدرج والإدخال والمراجعة               | **مكتملة**          | يومي/أسبوعي، طالب/مربي/ورقي، تاريخ نسخ، migrations `0016–0020`.       |
| P4      | التعلم والتكليفات والاختبارات وأسبوع الطالب     | **مكتملة تاريخيًا** | أُنجزت وظائف التعلم، ثم صححت P4.1 دورة الأسبوع.                       |
| P4.1    | الإغلاق الأسبوعي التلقائي وحفظ المراجعات        | **مغلقة**           | قبول حقيقي و123 اختبارًا، migrations `0024–0025`.                     |
| P5      | الانتباه والإجراءات والمتابعات والحالات         | **مغلقة**           | قبول حقيقي وثمانية أدلة، migrations `0026–0027`.                      |
| P6      | مركز المسؤول وأداء المربين والتقارير            | **لم تبدأ**         | لا حسابات أداء أو لوحات أو تصدير.                                     |
| P7      | إكمال تجربة الأدوار وRTL والهاتف والوصول        | **لم تبدأ**         | الواجهة الحالية تشغيلية أولية وليست تجربة المنتج النهائية.            |
| P8      | جاهزية الإنتاج وPilot                           | **لم تبدأ**         | لم تُنفذ بوابات الحمل والأمان والاستعادة والتجربة التشغيلية لأسبوعين. |

## 7. ما تم في P0 — الأساس الآمن

- إنشاء مستودع Git وتطبيق Next.js/TypeScript قابل للبناء والنشر.
- إنشاء `workspaces` و`persons` و`student_profiles` و`login_accounts` كأساس هوية مستقل عن الحساب.
- تطبيع اسم الدخول: Unicode NFC، تحويل الأرقام العربية والفارسية، قواعد طول ومحارف، وفرادة للاسم المطبع.
- محول دخول باسم مستخدم وكلمة مرور دون طلب بريد إلكتروني من المستخدم.
- حالات الحساب `PROVISIONING` و`ACTIVE` و`CREDENTIAL_UPDATE` و`DISABLED`.
- إنشاء الحساب وربطه بشخص موجود، وكلمة مرور مؤقتة، وإجبار التغيير، والتعطيل، وإعادة التفعيل، وإعادة التعيين الإداري.
- إثبات إبطال الجلسات القديمة بعد رفع `revoked_before`، مع قبول تسجيل دخول جديد لاحقًا.
- تخزين رموز الجلسة في Secure HttpOnly `__Host-` cookies وعدم إعادتها في JSON.
- rate limiting موزع داخل PostgreSQL باسم وشبكة مجزّأين بـHMAC.
- نموذج أخطاء ثابت، `request_id`، وسجلات منظمة لا تكشف تفاصيل المزود أو SQL.
- سجل `audit_events` تراكمي ومحمي من التعديل والحذف.
- أساس منع التكرار والتزامن، مع فشل مغلق عند غياب سياق قاعدة البيانات.
- `/health` لحياة التطبيق و`/ready` لاتصال قاعدة البيانات ودور التشغيل وآخر migration.
- إثبات عزل جهتين، ونطاق المسؤول والمربي والطالب، وعدم تسرب سياق الاتصال المجمّع.

## 8. ما تم في P1 — البرنامج والتوزيع

- قوالب برامج وخطط ذات نسخ وحالة `DRAFT/PUBLISHED`.
- أسابيع خطة، ونسخ غير قابلة للتغيير من الخطة المنشورة إلى الدفعة.
- دفعات ومجموعات وتسجيل طلاب.
- عضوية مجموعة مؤرخة بوقت السريان.
- إسناد مربي أساسي واحد للمجموعة في اللحظة نفسها، مع تاريخ كامل للنقل.
- قيود PostgreSQL تمنع التداخل الزمني وتُسلسل محاولات النقل المتزامنة.
- المسؤول يكتب داخل الجهة؛ المربي يرى ما أُسند إليه؛ الطالب يرى تسجيله ومجموعته فقط.
- قبول حقيقي أن تعديل القالب لاحقًا لا يغير نسخة الدفعة السابقة.

## 9. ما تم في P2 — الجلسات التربوية

- تعريفات جلسات ومقاييس مكتوبة النوع ومرتبطة بأسابيع الخطة.
- نسخ التعريفات والمقاييس إلى خطة الدفعة.
- توليد جلسة فعلية لكل تعريف/مجموعة مع مفتاح فرادة يمنع التكرار.
- دورة `PLANNED → OPEN → CLOSED`، وإلغاء من المخطط أو المفتوح بسبب موثق.
- تجميد roster الطلاب والمربي الأساسي عند فتح الجلسة وفق السريان عند موعدها.
- حالات حضور واضحة ومقاييس منطبقة بحسب حالة الحضور.
- حفظ جزئي ذري حتى 200 طالب، ثم منع الإغلاق مع أي بيانات مطلوبة ناقصة.
- إغلاق آمن أمام التكرار والتزامن؛ الجلسة المغلقة لا تُفتح من جديد.
- تصحيح بعد الإغلاق يحفظ before/after والسبب والمنفذ والطلب.
- قدرة المسؤول على إكمال الجلسة دون تغيير نسبة المربي التاريخية.

## 10. ما تم في P3 — التتبع والتدرج

- تعريفات للأوراد والعادات والقيم والأهداف.
- أنواع قيم: boolean، count، percent، score، duration، number، enum، ونص قصير.
- حدود رقمية وخيارات enum وسياسات مصدر ومراجعة وأوزان تقدم.
- جداول يومية أو أسبوعية بنطاق أسابيع وأيام استحقاق ووقت استحقاق.
- اشتقاق المطلوب وفق تاريخ بدء الدفعة والمنطقة الزمنية دون إنشاء قيمة صفرية للمفقود.
- إدخال الطالب لنفسه، وإدخال المربي الفردي أو الجماعي، وتفريغ الورقي وفق سياسة التعريف.
- batch ذري حتى 200 سجل.
- نسخة غير قابلة للتغيير لكل تعديل دلالي، ومراجعة مرتبطة برقم نسخة الإدخال.
- قرارات مراجعة `VERIFIED` أو `NEEDS_CORRECTION`، وإعادة المراجعة إلى الانتظار بعد التعديل.
- رفض نسخة الصف أو نسخة الإدخال القديمة بـ`VERSION_CONFLICT`.
- واجهة أولية للطالب والمربي للتتبع.

## 11. ما تم في P4 — التعلم وأسبوع الطالب

- محتوى وتكليفات واختبارات مرتبطة بأسبوع الخطة وتُنسخ إلى الدفعة.
- المحتوى غير المنشور مخفي، والنشر الصريح مدقق.
- تسليم طالب واحد حالي لكل تكليف، مع نسخ تاريخية عند التعديل.
- مراجعة المربي بدرجة لا تتجاوز الحد المنسوخ، وملاحظات داخلية منفصلة عن تغذية الطالب الراجعة.
- نتيجة اختبار تبدأ `DRAFT` ولا يراها الطالب حتى النشر.
- تصحيح النتيجة بسبب موثق ينشئ نسخة جديدة ويلزم نشرها مرة أخرى.
- تقييم ذاتي للطالب بدرجة محدودة وتأمل، مع تاريخ نسخ لا يُمحى.
- ملخص أسبوع يجمع الجلسات والتتبع والتكليفات والاختبارات والتقييم الذاتي.
- حساب تغطية = الحقائق المكتملة ÷ الحقائق المتوقعة المنطبقة.
- تطبيع الأدلة الرقمية مقابل الهدف/الحد ودمجها بأوزان الخطة المنسوخة.
- حفظ نسخة القواعد والأدلة داخل snapshot تاريخي.
- إثبات قبول حقيقي لدرجة 80 ثم 90 بعد التصحيح، مع بقاء النسخة الأولى ورفض row version قديم.
- واجهة تشغيلية للتعلم والأسبوع.

### تصحيح P4.1 المغلق

عالجت P4.1 تعارض P4 التاريخي مع القفل النهائي. دورة الأسبوع الفعلية الآن `OPEN → FINALIZED`، وحُذفت مسارات `ready/approve/corrections` الأسبوعية القديمة واستُبدلت بـ`finalize/amend`. يحفظ الإغلاق أول لقطة ومراجعتها، ويُنشئ التعديل اللاحق مراجعة جديدة بسبب موثق دون تغيير التاريخ. المسؤول يراجع الاستثناءات والتصحيحات بدل اعتماد كل طالب كل أسبوع.

## 12. واجهات API المنفذة فعليًا

### الأساس والدخول

- `POST /api/v1/auth/login`
- `GET /health`
- `GET /ready`

### البرنامج والتوزيع

- `GET/POST /api/v1/program-templates`
- `POST /api/v1/program-templates/:templateId/plans`
- `GET /api/v1/programs`
- `POST /api/v1/cohorts`
- `POST /api/v1/enrollments`
- `POST /api/v1/group-memberships`
- `POST /api/v1/mentor-assignments`

### الجلسات

- `GET /api/v1/sessions`
- `GET /api/v1/sessions/:sessionId`
- `POST /api/v1/sessions/:sessionId/open`
- `PUT /api/v1/sessions/:sessionId/records`
- `POST /api/v1/sessions/:sessionId/close`
- `POST /api/v1/sessions/:sessionId/cancel`
- `POST /api/v1/sessions/:sessionId/corrections`

### التتبع

- `GET /api/v1/tracking/expected`
- `PUT /api/v1/tracking/entries`
- `POST /api/v1/tracking/entries/batch`
- `POST /api/v1/tracking/entries/:entryId/reviews`
- `POST /api/v1/tracking/entries/:entryId/corrections`

### التعلم والأسبوع

- `GET /api/v1/learning`
- `POST /api/v1/content/:contentId/publish`
- `GET /api/v1/assignments`
- `PUT /api/v1/assignments/:assignmentId/submission`
- `POST /api/v1/submissions/:submissionId/review`
- `GET /api/v1/exams`
- `PUT /api/v1/exams/:examId/results`
- `POST /api/v1/exams/results/:resultId/publish`
- `POST /api/v1/exams/:examId/corrections`
- `PUT /api/v1/me/weeks/:weekId/self-review`
- `GET /api/v1/students/:studentId/weeks/:weekId`
- `POST /api/v1/students/:studentId/weeks/:weekId/finalize`
- `POST /api/v1/students/:studentId/weeks/:weekId/amend`

### المتابعة والانتباه والحالات

- `GET /api/v1/attention`
- `POST /api/v1/attention/evaluate`
- `POST /api/v1/attention/:attentionId/claim|snooze|resolve|dismiss`
- `GET/POST /api/v1/actions`
- `POST /api/v1/actions/:actionId/start|complete|verify|reschedule|cancel`
- `GET/POST /api/v1/students/:studentId/followups`
- `POST /api/v1/followups/:followupId/corrections`
- `GET/POST /api/v1/cases`
- `GET /api/v1/cases/:caseId`
- `POST /api/v1/cases/:caseId/events|assign|escalate|resolve|reopen|archive`

المخزون الكامل المقترح في التصميم أوسع من هذه القائمة. وجود مسار في التصميم لا يعني أنه منفذ ما لم يظهر هنا وفي `src/app/api/v1`.

## 13. قاعدة البيانات والمهاجرات

طُبقت migrations من `0001` إلى `0027` على مشروع Supabase الحالي:

| النطاق                       | migrations  |
| ---------------------------- | ----------- |
| P0: الهوية والجلسات وRLS     | `0001–0005` |
| P1: البرنامج والتوزيع        | `0006–0008` |
| P2: الجلسات وسجلاتها         | `0009–0015` |
| P3: التتبع والمراجعة         | `0016–0020` |
| P4: التعلم والأسبوع والتاريخ | `0021–0023` |
| P4.1: الإغلاق التلقائي       | `0024–0025` |
| P5: المتابعة والحالات        | `0026–0027` |

أهم عائلات الجداول المنفذة:

- الهوية: الجهات، الأشخاص، ملفات الطلاب، حسابات الدخول.
- الأمان والتشغيل: التدقيق، مفاتيح منع التكرار، محاولات الدخول، سياق النطاق.
- البرنامج: القوالب، الخطط، الأسابيع، الدفعات، المجموعات، التسجيلات، العضويات، وإسنادات المربين.
- الجلسات: التعريفات، المقاييس، الوقائع، roster، الحضور، القيم، وسجل التصحيحات.
- التتبع: التعريفات، الجداول، الإدخالات الحالية، المراجعات، ونسخ التصحيح.
- التعلم: المحتوى، تعريفات التكليف والاختبار، التسليمات ونسخها، النتائج ونسخها، التقييم الذاتي ونسخه، وملخصات الأسبوع ولقطاتها.
- المتابعة: التنبيهات، الإجراءات، المتابعات ونسخ تصحيحها، الحالات، وسجل أحداثها.

جميع جداول المجال ذات الصلة محمية بـRLS إجباري، وأدوار Supabase Data API العامة لا تملك صلاحيات مباشرة عليها.

## 14. الاختبارات وأدلة الإغلاق الحالية

الحالة الموثقة بعد P5:

- **126 اختبارًا ناجحًا** عبر 11 ملف unit/integration.
- `npm run format:check` ناجح.
- `npm run lint` ناجح دون تحذيرات مسموحة.
- `npm run typecheck` ناجح.
- `npm test` ناجح.
- `npm run build` ناجح.
- `npm run smoke` ناجح على ناتج البناء.
- قبول حقيقي منفصل لكل من P0 وP1 وP2 وP3 وP4 وP5 على Supabase/Vercel.
- `/health` و`/ready` ناجحان على النشر الحالي.
- GitHub Actions نجح عند إغلاق P5؛ تشغيل التنفيذ الأخير هو `34435878343`.

ملفات القبول الحقيقية داخل `output/` محلية ومستبعدة من Git، ولا تحتوي إلا booleans وتوقيتًا. سكربتات القبول تحذف مستخدمي Auth والصفوف التجريبية التي أنشأتها بالمعرفات الدقيقة.

## 15. ما تم في P5 — المتابعة والانتباه والحالات

أُنجزت الوحدة التالية واختُبرت على Vercel وSupabase الحقيقيين:

- `Attention` بالحالات `OPEN / IN_PROGRESS / SNOOZED / RESOLVED / DISMISSED`، ومنع التكرار لنفس القاعدة والدليل.
- `Action` بالحالات `OPEN / IN_PROGRESS / DONE_PENDING_VERIFICATION / VERIFIED / CANCELLED` مع فصل التنفيذ عن تحقق النتيجة.
- `Followup` كواقعة حدثت فعلًا، وتصحيحها بمراجعة محفوظة غير قابلة للتعديل، وإلغاؤها مدقق.
- `Case` بالحالات `OPEN / MONITORING / RESOLVED / ARCHIVED` مع سجل أحداث وملكية وأولوية.
- قواعد الانتباه الأولى: غيابان متتاليان بلا عذر، 14 يومًا بلا متابعة مؤهلة، طالب جديد بلا تفقد خلال 3 أيام، جلسة متأخرة أكثر من 24 ساعة، اختبار/مراجعة متأخر، أو إجراء حالة متأخر/مفقود.
- التحقق من النتيجة قبل حل الحالة؛ تنفيذ اتصال وحده لا يكفي.
- الإسناد للمربي الفعلي وقت الاستحقاق، والتصعيد، والعزل بين الجهات، ومنع الطالب من الموارد الداخلية.
- قبول إنتاجي بثمانية أدلة منزوعة الأسرار، مع تنظيف fixtures الدقيقة.

المتابعة القادمة هي `Action` بموعد، وليست `Followup` مزعومة قبل حدوثها. لا تُفتح Case تلقائيًا في V1.

## 16. ما لم يُنفذ بعد داخل النطاق الكامل

### P6 — مركز المسؤول والأداء والتقارير

لم تُنفذ بعد:

- لوحة نواقص الفريق مع drill-down إلى الأدلة.
- أداء المربي بالأوزان المعتمدة: 35% التزام تشغيلي، 30% متابعة، 20% اكتمال بيانات، 15% استجابة للحالات.
- استبعاد الأبعاد ذات المقام صفر، وإظهار “بيانات محدودة” عند أقل من خمس فرص.
- عدم إدخال درجات الطلاب في تقييم المربي.
- اتجاهات متوافقة مع نسخة قواعد القياس.
- تقارير الطالب والمجموعة والمربي والبرنامج، والتصدير المحدود المدقق.

### P7 — تجربة الأدوار

لم تُنفذ بعد تجربة المنتج النهائية:

- صفحات اليوم والأسبوع والتقدم الموحّدة لكل دور.
- تنقل كامل للمسؤول والمربي والطالب.
- تحسين شامل للهاتف وRTL والوصول بلوحة المفاتيح وقارئات الشاشة.
- حالات التحميل والفراغ والخطأ والاستعادة في جميع الرحلات.
- تنبيهات الواجهة وتجربة التصحيح والتدقيق.

الواجهات الحالية تثبت العمليات الأساسية، لكنها ليست معيار إغلاق P7.

### P8 — الإنتاج والتجربة الحقيقية

لم تُنفذ بعد:

- فصل واضح ونهائي بين Local وStaging وProduction.
- اختبارات حمل وسعة واتصالات مجمعة وحدود Vercel/Supabase.
- مراجعة أمان مستقلة واختبارات اختراق موجهة.
- نسخ احتياطي واستعادة مجربة فعليًا، لا مجرد تفعيل النسخ.
- مراقبة وتنبيه وسجل تشغيل وحوادث.
- runbook للنشر والتراجع واستعادة الخدمة وتدوير الأسرار.
- تجربة Pilot حقيقية ناجحة لمدة أسبوعين ومعايير خروج موثقة.

### عناصر من مخزون V1 لم تُكملها المراحل الحالية

- logout، تغيير كلمة المرور ذاتيًا، و`GET /me` كواجهات عامة مكتملة.
- CRUD الكامل للحسابات والأشخاص والطلاب والجهة.
- تعديل/أرشفة تعريفات الخطة بعد بناء دورة revisions المناسبة.
- دورة حياة الدفعة الكاملة وخطط تعديل المستقبل وplan diff/apply.
- صفحات تفاصيل مستقلة لكل تكليف واختبار كما في المخزون الأصلي.
- `/me/today` و`/me/program` و`/me/progress` بصورتها النهائية.
- التغذية الراجعة العامة والملاحظات الداخلية كموارد عامة مستقلة.
- التدقيق القابل للاستعراض للمسؤول والتقارير والتصدير.

## 17. وضع البيئات والنشر الآن

اختار مالك المشروع استخدام مشروع Supabase واحد بدأ عليه الاختبار وسيكون مشروع الإنتاج الأولي، مع تنظيف fixtures بعد القبول. Vercel مرتبط بالمستودع وينشر الفرع الرئيسي على الرابط الحالي.

هذا كافٍ لإثبات المراحل الحالية، لكنه لا يحقق بعد عزل البيئات المطلوب للإنتاج الناضج. قبل Pilot يجب إنشاء فصل واضح للبيانات والإعدادات بين التطوير/التجريب والإنتاج أو اعتماد سياسة بديلة موثقة تمنع الاختبارات من لمس بيانات حقيقية.

ملفات `.env*` المحلية مستبعدة من Git باستثناء قالب خالٍ من الأسرار. لا تُكتب القيم السرية في هذا المستند أو README أو سجلات القبول.

## 18. المخاطر والديون الحالية

| الأولوية       | البند                                                        | الإجراء المطلوب                                                                         |
| -------------- | ------------------------------------------------------------ | --------------------------------------------------------------------------------------- |
| حرجة قبل Pilot | بعض بيانات الاتصال ظهرت في صور الإعداد خلال العمل            | تدوير كل سر ظهر، وتحديث Supabase/Vercel والملف المحلي، ثم إعادة اختبارات الصحة والقبول. |
| عالية          | مشروع Supabase واحد للاختبار والإنتاج الأولي                 | فصل البيئات أو اعتماد ضوابط اختبار صارمة قبل وجود بيانات حقيقية.                        |
| عالية          | النسخ والاستعادة غير مثبتين                                  | تنفيذ اختبار backup/restore موثق في P8.                                                 |
| عالية          | لا توجد مراقبة وتنبيهات تشغيلية كاملة                        | إضافة observability وتنبيهات دون تسجيل بيانات حساسة.                                    |
| متوسطة         | الواجهة عملية وليست نهائية                                   | إغلاق تجربة الأدوار والوصول والهاتف في P7.                                              |
| متوسطة         | مخزون REST الكامل غير منفذ                                   | تنفيذ المسارات عند مرحلة مجالها، وعدم إعلانها متاحة قبل الاختبار.                       |
| متوسطة         | GitHub Actions يستخدم `actions/checkout@v4` و`setup-node@v4` | متابعة إشعارات deprecation وترقية الإصدارات قبل P8 إن لزم.                              |

## 19. الترتيب الصحيح للاستكمال

1. **P6:** مركز المسؤول وأداء المربين والتقارير المبنية على أدلة قابلة للفتح.
2. **P7:** الرحلات الكاملة للأدوار، الهاتف، RTL، والوصول.
3. **P8:** فصل البيئات، الحمل، الأمان، الاستعادة، المراقبة، runbook، ثم Pilot لأسبوعين.

بوابة P5 المثبتة تشمل:

- إنشاء Attention واحد فقط لنفس القاعدة والدليل رغم إعادة التقييم.
- claim/snooze/resolve/dismiss بانتقالات صحيحة وأسباب محفوظة.
- Action بموعد، ثم تنفيذ، ثم تحقق مستقل من النتيجة.
- Followup لا يُنشأ قبل حدوثه، وتصحيحه يحفظ النسخة السابقة.
- Case لا تُفتح تلقائيًا، ولا تُحل دون تحقق وتفسير.
- نسبة التأخير إلى المربي المسؤول في وقت الاستحقاق، لا إلى المربي الحالي فقط.
- عزل جهتين ونطاق المربي وخصوصية الطالب ورفض Data API المباشر.
- idempotency وrow-version والتدقيق والتنظيف والقبول الحقيقي.

## 20. التشغيل والتحقق للمطور

المتطلبات: Node.js 24 وملف `.env.local` محلي صحيح.

```sh
npm ci
npm run dev:runtime
```

ثم افتح `http://localhost:3000`.

الفحوص المحلية المطلوبة قبل كل push:

```sh
npm run format:check
npm run lint
npm run typecheck
npm test
npm run build
npm run smoke
```

عمليات قاعدة البيانات، من جهاز مشغّل مصرح فقط:

```sh
npm run db:migrate
npm run db:setup-runtime
npm run db:verify-runtime
```

القبول الحقيقي حسب المرحلة:

```sh
npm run acceptance:session
npm run acceptance:p1
npm run acceptance:p2
npm run acceptance:p3
npm run acceptance:p4
npm run acceptance:p5
```

يجب عدم تشغيل migration أو قبول حقيقي على بيئة تحتوي بيانات مستخدمين دون فهم نطاق السكربت وإثبات أن تنظيفه يعتمد معرفات fixtures الدقيقة.

## 21. تعريف الإنجاز النهائي للمشروع

لا يعد المشروع كاملًا عند اكتمال P5. يكتمل V1 عندما:

- تنجح P6–P7 بعقودها وقبولها الحقيقي وعزلها وصلاحياتها.
- تنجح بوابات P8: الحمل، الأمان، الاستعادة، المراقبة، والنشر والتراجع.
- يمر Pilot لمدة أسبوعين ببيانات تشغيل حقيقية دون تسرب أو فقد تاريخ أو عجز تشغيلي حرج.
- تصبح وثائق التشغيل والاستعادة وتدوير الأسرار ومسؤوليات الدعم قابلة للتنفيذ من شخص غير كاتب الكود.

## 22. خريطة الملفات المرجعية

- `README.md`: نقطة البدء والتشغيل المحلي.
- `docs/p0-implementation-brief.md`: منهجية وبوابات الأساس.
- `docs/p0-schema.md`: عقد مخطط P0.
- `docs/p0-auth-and-scope.md`: حالات الحساب والصلاحيات والنطاق.
- `docs/p0-setup.md`: إعداد البيئة والتجارب الحقيقية.
- `docs/p0-status.md`: دليل إغلاق P0.
- `docs/p1-contract.md` و`docs/p1-status.md`: عقد ودليل البرنامج والتوزيع.
- `docs/p2-contract.md` و`docs/p2-status.md`: عقد ودليل الجلسات.
- `docs/p3-contract.md` و`docs/p3-status.md`: عقد ودليل التتبع.
- `docs/p4-contract.md` و`docs/p4-status.md`: عقد ودليل التعلم والأسبوع، مع الانحراف الموضح هنا.
- `docs/p4.1-contract.md` و`docs/p4.1-status.md`: عقد ودليل إغلاق تصحيح دورة الأسبوع.
- `docs/p5-contract.md` و`docs/p5-status.md`: عقد ودليل المتابعة والانتباه والحالات.
- `db/migrations/`: التاريخ التنفيذي لقاعدة البيانات من `0001` إلى `0027`.
- `src/application/`: قواعد المجال والخدمات.
- `src/app/api/v1/`: واجهات HTTP المنفذة فعليًا.
- `tests/`: اختبارات الوحدة والتكامل.
- `.github/workflows/ci.yml`: بوابة CI المحلية على GitHub.

## 23. قاعدة تحديث هذا المستند

بعد كل مرحلة أو قرار منتج مؤثر يجب تحديث:

1. سطر الحالة والتاريخ وآخر commit تم التحقق منه.
2. جدول المراحل.
3. قائمة API والمهاجرات المنفذة فعليًا.
4. أدلة الاختبار والنشر.
5. المخاطر والانحرافات وما أُغلق منها.
6. الخطوة التالية وبوابة قبولها.

لا تُستبدل كلمة “لم يبدأ” بتقدير نسبة غير قابلة للإثبات. إما أن يوجد عقد/كود/migration/اختبار/قبول محدد، أو يبقى العنصر غير منفذ.
